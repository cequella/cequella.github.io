export interface Sketch {
    metadata: {
        id: string;
        image: string;
        title: string;
        description: string;
    };
    setup(canvas: HTMLCanvasElement): void;
    destroy(): void;
    resize(width: number, height: number): void;
}
